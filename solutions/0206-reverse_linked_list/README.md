# 206. Reverse Linked List

`Easy` · `Recursion`

🔗 **[Problem on LeetCode](https://leetcode.com/problems/reverse-linked-list/)**

## Problem

Given the head of a singly linked list, reverse the list, and return the reversed list. Solve it both iteratively and recursively.

## Solution

See [`solution.py`](./solution.py) — verified by [`test_solution.py`](./test_solution.py).

```bash
python -m pytest test_solution.py
```

Primary function: `reverse_list`
