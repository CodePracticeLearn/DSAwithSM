# 239. Sliding Window Maximum

`Hard` · `Sliding Window`

🔗 **[Problem on LeetCode](https://leetcode.com/problems/sliding-window-maximum/)**

## Problem

Given an array nums and window size k, return a list of the maximum value in each sliding window as it moves from left to right.

## Solution

See [`solution.py`](./solution.py) — verified by [`test_solution.py`](./test_solution.py).

```bash
python -m pytest test_solution.py
```

Primary function: `max_sliding_window`
